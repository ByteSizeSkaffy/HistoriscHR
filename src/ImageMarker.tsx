import React from 'react';
import {useState} from 'react';

type MarkerProps={
    ImageOld:string;
    ImageMiddle:string;
    ImageNew:string;
}

const Marker: React.FC<MarkerProps> = ({ImageOld, ImageMiddle, ImageNew}) => {

    return(
        <div></div>
    )
}

export default Marker;
export type {};